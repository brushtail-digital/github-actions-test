name: Build

on:
  push:
    branches: [master]
    
jobs:
  build:
    name: Test build environment
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          echo $PATH
        shell: powershell -Command "& $GITHUB_WORKSPACE/scripts/msys2 {0}"
